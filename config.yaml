#!/usr/bin/env python3
# Diamond-Pigs Trend v4 - Bitvavo via ccxt
# - Vast of automatisch scanbare EUR-markten
# - Max aantal open posities
# - Alleen bot-posities worden verkocht
# - Netto PnL inclusief buy- en sell-fees
# - Minimale netto winstfilter voor winst-exits
# - Cooldown blokkeert alleen BUY, nooit SELL

import os
import time
import json
import math
import logging
from datetime import datetime, timezone
from pathlib import Path
from typing import Any, Dict, Tuple, List

import pandas as pd
import yaml
from dotenv import load_dotenv
import ccxt

LOG = logging.getLogger("diamond")

# ------------------ Helpers ------------------

def now_iso() -> str:
    return datetime.now(timezone.utc).astimezone().isoformat(timespec="seconds")

def to_bool(v, default=False) -> bool:
    if isinstance(v, bool):
        return v
    if v is None:
        return default
    if isinstance(v, (int, float)):
        return v != 0
    if isinstance(v, str):
        s = v.strip().lower()
        return s in ("true", "waar", "yes", "ja", "1", "on", "aan", "y")
    return default

def to_float(v, default=0.0) -> float:
    if v is None:
        return default
    if isinstance(v, (int, float)):
        return float(v)
    if isinstance(v, str):
        s = v.strip().replace("%", "").replace(",", ".")
        try:
            return float(s)
        except ValueError:
            return default
    return default

def to_int(v, default=0) -> int:
    if v is None:
        return default
    if isinstance(v, int):
        return v
    if isinstance(v, float):
        return int(v)
    if isinstance(v, str):
        s = v.strip().replace(",", ".")
        try:
            return int(float(s))
        except ValueError:
            return default
    return default

def fmt_eur(x: float) -> str:
    return f"{x:,.2f}".replace(",", "X").replace(".", ",").replace("X", ".")

def timeframe_to_seconds(tf: str) -> int:
    tf = str(tf).strip().lower()
    if tf.endswith("m"):
        return int(tf[:-1]) * 60
    if tf.endswith("h"):
        return int(tf[:-1]) * 3600
    if tf.endswith("d"):
        return int(tf[:-1]) * 86400
    return 900

# ------------------ Indicators ------------------

def sma(series: pd.Series, length: int) -> pd.Series:
    return series.rolling(length).mean()

def rsi(series: pd.Series, length: int = 14) -> pd.Series:
    delta = series.diff()
    up = delta.clip(lower=0)
    down = -delta.clip(upper=0)
    roll_up = up.rolling(length).mean()
    roll_down = down.rolling(length).mean()
    rs = roll_up / (roll_down.replace(0, 1e-12))
    return 100 - (100 / (1 + rs))

def atr(df: pd.DataFrame, length: int = 14) -> pd.Series:
    high = df["high"]
    low = df["low"]
    close = df["close"]
    prev_close = close.shift(1)
    tr = pd.concat(
        [(high - low).abs(), (high - prev_close).abs(), (low - prev_close).abs()],
        axis=1,
    ).max(axis=1)
    return tr.rolling(length).mean()

# ------------------ Files ------------------

ROOT = Path(__file__).parent
CFG_FILE = Path(os.getenv("CFG_FILE", str(ROOT / "config.yaml")))
STATE_FILE = Path(os.getenv("STATE_FILE", str(ROOT / "state.json")))
TX_CSV = ROOT / "transactions.csv"

def setup_logging(level: str):
    level = (level or "INFO").upper()
    logging.basicConfig(
        level=getattr(logging, level, logging.INFO),
        format="%(asctime)s %(levelname)s %(message)s",
        datefmt="%Y-%m-%d %H:%M:%S",
    )

# ------------------ Config ------------------

ALLOWED_TOP_KEYS = {"quote", "symbols", "timeframe", "scanner", "signals", "risk", "fees", "logging"}
ALLOWED_SCANNER_KEYS = {"auto_scan", "top_n_markets", "min_quote_volume", "exclude_bases"}
ALLOWED_SIGNALS_KEYS = {
    "use_sma", "sma_fast", "sma_slow",
    "use_rsi", "rsi_len", "rsi_buy_min",
    "use_atr", "atr_len", "atr_tp_mult", "atr_sl_mult",
    "exit_on_trend_break",
    "use_atr_filter", "min_atr_pct",
}
ALLOWED_RISK_KEYS = {
    "fixed_stake_quote", "max_open_positions", "only_buy_if_not_in_position",
    "cooldown_minutes", "max_spread_pct", "eur_reserve",
    "dry_run", "skip_log_every_seconds", "min_profit_eur",
}
ALLOWED_FEES_KEYS = {"taker_fee_pct"}
ALLOWED_LOG_KEYS = {"level", "loop_sleep_seconds", "candles_limit"}

def _assert_no_unknown(section: str, data: Any, allowed: set):
    if not isinstance(data, dict):
        return
    unknown = [k for k in data.keys() if k not in allowed]
    if unknown:
        raise ValueError(f"Config error: unknown keys in '{section}': {sorted(unknown)}")

def validate_cfg(cfg: Dict[str, Any]):
    sc = cfg["scanner"]
    sig = cfg["signals"]
    rk = cfg["risk"]
    lg = cfg["logging"]

    if not cfg["quote"]:
        raise ValueError("Config error: quote may not be empty.")
    if sig["use_sma"] and sig["sma_fast"] >= sig["sma_slow"]:
        raise ValueError("Config error: sma_fast must be smaller than sma_slow.")
    if rk["fixed_stake_quote"] <= 0:
        raise ValueError("Config error: fixed_stake_quote must be > 0.")
    if rk["max_open_positions"] <= 0:
        raise ValueError("Config error: max_open_positions must be > 0.")
    if rk["max_spread_pct"] <= 0:
        raise ValueError("Config error: max_spread_pct must be > 0.")
    if rk["min_profit_eur"] < 0:
        raise ValueError("Config error: min_profit_eur must be >= 0.")
    if lg["loop_sleep_seconds"] <= 0:
        raise ValueError("Config error: loop_sleep_seconds must be > 0.")
    if lg["candles_limit"] < 100:
        raise ValueError("Config error: candles_limit must be >= 100.")
    if sc["top_n_markets"] <= 0:
        raise ValueError("Config error: top_n_markets must be > 0.")
    if sc["min_quote_volume"] < 0:
        raise ValueError("Config error: min_quote_volume must be >= 0.")

def load_cfg() -> Dict[str, Any]:
    if not CFG_FILE.exists():
        raise FileNotFoundError(f"Config not found: {CFG_FILE}")

    raw = yaml.safe_load(CFG_FILE.read_text(encoding="utf-8")) or {}
    if not isinstance(raw, dict):
        raise ValueError("Config must be a YAML mapping.")

    _assert_no_unknown("root", raw, ALLOWED_TOP_KEYS)

    cfg = dict(raw)
    cfg.setdefault("quote", "EUR")
    cfg.setdefault("symbols", ["BTC", "ETH", "XRP", "SOL", "TAO"])
    cfg.setdefault("timeframe", "15m")
    cfg.setdefault("scanner", {})
    cfg.setdefault("signals", {})
    cfg.setdefault("risk", {})
    cfg.setdefault("fees", {})
    cfg.setdefault("logging", {})

    sc = cfg["scanner"]
    sig = cfg["signals"]
    rk = cfg["risk"]
    fees = cfg["fees"]
    lg = cfg["logging"]

    _assert_no_unknown("scanner", sc, ALLOWED_SCANNER_KEYS)
    _assert_no_unknown("signals", sig, ALLOWED_SIGNALS_KEYS)
    _assert_no_unknown("risk", rk, ALLOWED_RISK_KEYS)
    _assert_no_unknown("fees", fees, ALLOWED_FEES_KEYS)
    _assert_no_unknown("logging", lg, ALLOWED_LOG_KEYS)

    sc.setdefault("auto_scan", False)
    sc.setdefault("top_n_markets", 5)
    sc.setdefault("min_quote_volume", 0)
    sc.setdefault("exclude_bases", ["EUR", "USDT", "USDC"])

    sig.setdefault("use_sma", True)
    sig.setdefault("sma_fast", 20)
    sig.setdefault("sma_slow", 60)
    sig.setdefault("use_rsi", True)
    sig.setdefault("rsi_len", 14)
    sig.setdefault("rsi_buy_min", 60)
    sig.setdefault("use_atr", True)
    sig.setdefault("atr_len", 14)
    sig.setdefault("atr_tp_mult", 3.0)
    sig.setdefault("atr_sl_mult", 1.2)
    sig.setdefault("exit_on_trend_break", False)
    sig.setdefault("use_atr_filter", True)
    sig.setdefault("min_atr_pct", 0.35)

    rk.setdefault("fixed_stake_quote", 30.0)
    rk.setdefault("max_open_positions", 5)
    rk.setdefault("only_buy_if_not_in_position", True)
    rk.setdefault("cooldown_minutes", 60)
    rk.setdefault("max_spread_pct", 0.20)
    rk.setdefault("eur_reserve", 50.0)
    rk.setdefault("dry_run", False)
    rk.setdefault("skip_log_every_seconds", 900)
    rk.setdefault("min_profit_eur", 0.50)

    fees.setdefault("taker_fee_pct", 0.25)

    lg.setdefault("level", "INFO")
    lg.setdefault("loop_sleep_seconds", 60)
    lg.setdefault("candles_limit", 400)

    cfg["quote"] = str(cfg["quote"]).upper().strip()
    cfg["symbols"] = [str(x).upper().strip() for x in list(cfg["symbols"]) if str(x).strip()]
    cfg["timeframe"] = str(cfg["timeframe"]).strip()

    sc["auto_scan"] = to_bool(sc.get("auto_scan"), False)
    sc["top_n_markets"] = to_int(sc.get("top_n_markets"), 5)
    sc["min_quote_volume"] = to_float(sc.get("min_quote_volume"), 0)
    sc["exclude_bases"] = [str(x).upper().strip() for x in list(sc.get("exclude_bases", [])) if str(x).strip()]

    sig["use_sma"] = to_bool(sig.get("use_sma"), True)
    sig["sma_fast"] = to_int(sig.get("sma_fast"), 20)
    sig["sma_slow"] = to_int(sig.get("sma_slow"), 60)
    sig["use_rsi"] = to_bool(sig.get("use_rsi"), True)
    sig["rsi_len"] = to_int(sig.get("rsi_len"), 14)
    sig["rsi_buy_min"] = to_float(sig.get("rsi_buy_min"), 60)
    sig["use_atr"] = to_bool(sig.get("use_atr"), True)
    sig["atr_len"] = to_int(sig.get("atr_len"), 14)
    sig["atr_tp_mult"] = to_float(sig.get("atr_tp_mult"), 3.0)
    sig["atr_sl_mult"] = to_float(sig.get("atr_sl_mult"), 1.2)
    sig["exit_on_trend_break"] = to_bool(sig.get("exit_on_trend_break"), False)
    sig["use_atr_filter"] = to_bool(sig.get("use_atr_filter"), True)
    sig["min_atr_pct"] = to_float(sig.get("min_atr_pct"), 0.35)

    rk["fixed_stake_quote"] = to_float(rk.get("fixed_stake_quote"), 30.0)
    rk["max_open_positions"] = to_int(rk.get("max_open_positions"), 5)
    rk["only_buy_if_not_in_position"] = to_bool(rk.get("only_buy_if_not_in_position"), True)
    rk["cooldown_minutes"] = to_int(rk.get("cooldown_minutes"), 60)
    rk["max_spread_pct"] = to_float(rk.get("max_spread_pct"), 0.20)
    rk["eur_reserve"] = to_float(rk.get("eur_reserve"), 50.0)
    rk["dry_run"] = to_bool(rk.get("dry_run"), False)
    rk["skip_log_every_seconds"] = max(10, to_int(rk.get("skip_log_every_seconds"), 900))
    rk["min_profit_eur"] = to_float(rk.get("min_profit_eur"), 0.50)

    fees["taker_fee_pct"] = to_float(fees.get("taker_fee_pct"), 0.25)

    lg["level"] = str(lg.get("level", "INFO")).upper()
    lg["loop_sleep_seconds"] = to_int(lg.get("loop_sleep_seconds"), 60)
    lg["candles_limit"] = to_int(lg.get("candles_limit"), 400)

    validate_cfg(cfg)
    cfg["_cfg_path"] = str(CFG_FILE)
    return cfg

# ------------------ State & CSV ------------------

def load_state() -> Dict[str, Any]:
    if STATE_FILE.exists():
        try:
            st = json.loads(STATE_FILE.read_text(encoding="utf-8"))
            if isinstance(st, dict):
                st.setdefault("positions", {})
                st.setdefault("cooldown", {})
                st.setdefault("pnl_quote", 0.0)
                st.setdefault("trades", 0)
                st.setdefault("wins", 0)
                return st
        except Exception:
            pass
    return {"positions": {}, "cooldown": {}, "pnl_quote": 0.0, "trades": 0, "wins": 0}

def save_state(state: Dict[str, Any]):
    STATE_FILE.write_text(json.dumps(state, indent=2, ensure_ascii=False), encoding="utf-8")

def ensure_csv_header():
    if TX_CSV.exists():
        return
    cols = ["ts","market","side","price","base_amount","fees_quote","spread_pct","net_pnl_quote","holding_time_min","reason","dry_run"]
    pd.DataFrame([], columns=cols).to_csv(TX_CSV, index=False)

def append_tx(row: Dict[str, Any]):
    ensure_csv_header()
    pd.DataFrame([row]).to_csv(TX_CSV, index=False, mode="a", header=False)

# ------------------ Exchange ------------------

def make_exchange():
    api_key = os.getenv("BITVAVO_API_KEY", "").strip()
    api_secret = os.getenv("BITVAVO_API_SECRET", "").strip()
    if not api_key or not api_secret:
        raise RuntimeError("Missing BITVAVO_API_KEY / BITVAVO_API_SECRET.")

    ex = ccxt.bitvavo({
        "apiKey": api_key,
        "secret": api_secret,
        "enableRateLimit": True,
    })

    opid = os.getenv("BITVAVO_OPERATOR_ID", "").strip()
    if opid:
        try:
            ex.options = ex.options or {}
            ex.options["operatorId"] = int(opid)
        except Exception:
            LOG.warning("BITVAVO_OPERATOR_ID ignored (must be integer).")

    return ex

def safe_fetch_balance(ex, retries=5, base_sleep=1.0):
    last_err = None
    for i in range(retries):
        try:
            return ex.fetch_balance()
        except Exception as e:
            last_err = e
            sleep_s = base_sleep * (2 ** i)
            LOG.warning(f"Fetch balance failed ({i+1}/{retries}): {e}. Sleep {sleep_s:.1f}s")
            time.sleep(sleep_s)
    raise RuntimeError(f"Fetch balance failed after {retries} attempts: {last_err}")

def get_free_quote(balance: Dict[str, Any], quote: str) -> float:
    try:
        return float((balance.get("free", {}) or {}).get(quote, 0.0) or 0.0)
    except Exception:
        return 0.0

# ------------------ Market helpers ------------------

def market_symbol(base: str, quote: str) -> str:
    return f"{base}/{quote}"

def fetch_ticker(ex, market: str) -> Dict[str, Any]:
    return ex.fetch_ticker(market)

def calc_spread_pct(ticker: Dict[str, Any]) -> float:
    bid = ticker.get("bid")
    ask = ticker.get("ask")
    if not bid or not ask or bid <= 0:
        return 999.0
    return (ask - bid) / ask * 100.0

def fetch_candles(ex, market: str, timeframe: str, limit: int) -> pd.DataFrame:
    ohlcv = ex.fetch_ohlcv(market, timeframe=timeframe, limit=limit)
    if not ohlcv or len(ohlcv) < 50:
        raise RuntimeError("Not enough candle data.")
    df = pd.DataFrame(ohlcv, columns=["ts","open","high","low","close","volume"])
    df["ts"] = pd.to_datetime(df["ts"], unit="ms", utc=True)
    df = df.sort_values("ts").drop_duplicates(subset=["ts"], keep="last").reset_index(drop=True)
    if len(df) < 50:
        raise RuntimeError("Not enough clean candle data.")
    return df

def amount_to_precision_safe(ex, market: str, amount: float) -> float:
    try:
        return float(ex.amount_to_precision(market, amount))
    except Exception:
        return float(amount)

def market_limits(ex, market: str) -> Tuple[float, float]:
    mk = ex.market(market)
    min_amount = float((mk.get("limits", {}) or {}).get("amount", {}).get("min", 0) or 0)
    min_cost = float((mk.get("limits", {}) or {}).get("cost", {}).get("min", 0) or 0)
    return min_amount, min_cost

def fee_from_order_or_estimate(order: Dict[str, Any], fallback_quote_amount: float, taker_fee_pct: float) -> float:
    try:
        fee = order.get("fee")
        if isinstance(fee, dict) and fee.get("cost") is not None:
            return float(fee["cost"])
        fees = order.get("fees")
        if isinstance(fees, list) and fees:
            total = 0.0
            for f in fees:
                if isinstance(f, dict) and f.get("cost") is not None:
                    total += float(f["cost"])
            if total > 0:
                return total
    except Exception:
        pass
    return float(fallback_quote_amount) * (float(taker_fee_pct) / 100.0)

# ------------------ Scanner ------------------

def scan_markets(ex, quote: str, sc: Dict[str, Any]) -> List[str]:
    exclude = set([quote] + list(sc.get("exclude_bases", [])))
    eligible_markets = []

    for sym, mk in ex.markets.items():
        try:
            if mk.get("active", True) is False:
                continue
            if mk.get("spot") is False:
                continue
            if str(mk.get("quote", "")).upper() != quote:
                continue
            base = str(mk.get("base", "")).upper()
            if not base or base in exclude:
                continue
            eligible_markets.append(sym)
        except Exception:
            continue

    tickers = {}
    try:
        tickers = ex.fetch_tickers(eligible_markets)
    except Exception:
        for sym in eligible_markets:
            try:
                tickers[sym] = ex.fetch_ticker(sym)
            except Exception:
                continue
            time.sleep(max(0.05, ex.rateLimit / 1000.0))

    ranked = []
    for sym in eligible_markets:
        t = tickers.get(sym)
        if not t:
            continue
        last = to_float(t.get("last") or t.get("close"), 0.0)
        qv = to_float(t.get("quoteVolume"), 0.0)
        if qv <= 0:
            bv = to_float(t.get("baseVolume"), 0.0)
            if bv > 0 and last > 0:
                qv = bv * last
        if qv < sc["min_quote_volume"]:
            continue
        ranked.append((qv, sym))

    ranked.sort(reverse=True)

    bases = []
    for _, sym in ranked[:sc["top_n_markets"]]:
        try:
            base = str(ex.market(sym).get("base", sym.split("/")[0])).upper()
            if base not in bases:
                bases.append(base)
        except Exception:
            base = sym.split("/")[0].upper()
            if base not in bases:
                bases.append(base)

    return bases

# ------------------ Signals ------------------

def compute_signal(df: pd.DataFrame, cfg: Dict[str, Any]) -> Dict[str, Any]:
    sig = cfg["signals"]
    closes = df["close"]

    if sig["use_sma"]:
        df["sma_fast"] = sma(closes, sig["sma_fast"])
        df["sma_slow"] = sma(closes, sig["sma_slow"])
    else:
        df["sma_fast"] = float("nan")
        df["sma_slow"] = float("nan")

    if sig["use_rsi"]:
        df["rsi"] = rsi(closes, sig["rsi_len"])
    else:
        df["rsi"] = float("nan")

    if sig["use_atr"] or sig["use_atr_filter"]:
        df["atr"] = atr(df, sig["atr_len"])
    else:
        df["atr"] = 0.0

    last = df.iloc[-1]
    last_close = float(last["close"])

    trend_up = True
    trend_break = False
    if sig["use_sma"]:
        trend_up = (last["sma_fast"] > last["sma_slow"])
        trend_break = (last["sma_fast"] < last["sma_slow"])

    rsi_val = float(last["rsi"]) if sig["use_rsi"] and not math.isnan(float(last["rsi"])) else 50.0
    rsi_ok = (rsi_val >= sig["rsi_buy_min"]) if sig["use_rsi"] else True

    atr_val = float(last.get("atr", 0.0)) if not math.isnan(float(last.get("atr", 0.0))) else 0.0
    atr_pct = (atr_val / last_close * 100.0) if last_close > 0 else 0.0
    atr_filter_ok = True
    if sig["use_atr_filter"]:
        atr_filter_ok = (atr_pct >= sig["min_atr_pct"])

    return {
        "trend_up": bool(trend_up),
        "trend_break": bool(trend_break),
        "rsi": float(rsi_val),
        "rsi_ok": bool(rsi_ok),
        "atr": float(atr_val),
        "atr_pct": float(atr_pct),
        "atr_filter_ok": bool(atr_filter_ok),
        "last_close": float(last_close),
    }

def should_buy(signal: Dict[str, Any], in_position: bool, cfg: Dict[str, Any]) -> Tuple[bool, str, str]:
    sig = cfg["signals"]
    if in_position:
        return False, "ALREADY_IN_POSITION", "Already in position"
    if sig["use_sma"] and not signal["trend_up"]:
        return False, "NO_UPTREND", "SKIP no uptrend"
    if sig["use_rsi"] and not signal["rsi_ok"]:
        return False, "RSI_LOW", f"SKIP RSI {signal['rsi']:.1f} < {sig['rsi_buy_min']}"
    if sig["use_atr_filter"] and not signal["atr_filter_ok"]:
        return False, "ATR_LOW", f"SKIP ATR% {signal['atr_pct']:.3f} < {sig['min_atr_pct']}"
    return True, "BUY_OK", "BUY signal"

def should_sell(signal: Dict[str, Any], pos: Dict[str, Any], cfg: Dict[str, Any], last_price: float) -> Tuple[bool, str]:
    sig = cfg["signals"]
    entry = float(pos["entry_price"])

    atr_val = float(signal.get("atr", 0.0) or 0.0)
    if atr_val <= 0:
        atr_val = max(entry * 0.002, 0.01)

    tp = entry + sig["atr_tp_mult"] * atr_val
    sl = entry - sig["atr_sl_mult"] * atr_val

    if last_price >= tp:
        return True, "SELL ATR_TP"
    if last_price <= sl:
        return True, "SELL ATR_SL"
    if sig["exit_on_trend_break"] and signal.get("trend_break", False):
        return True, "SELL TREND_BREAK"
    return False, "HOLD"

def estimate_net_pnl_at_price(pos: Dict[str, Any], price: float, taker_fee_pct: float) -> float:
    base_amount = float(pos.get("base_amount", 0.0))
    entry_cost = float(pos.get("entry_cost_quote", 0.0))
    entry_fee = float(pos.get("entry_fee_quote", 0.0))
    proceeds = base_amount * price
    sell_fee = proceeds * (taker_fee_pct / 100.0)
    return (proceeds - entry_cost) - (entry_fee + sell_fee)

# ------------------ Orders ------------------

def place_market_buy(ex, market: str, stake_quote: float, taker_fee_pct: float, dry_run: bool):
    ticker = fetch_ticker(ex, market)
    ask = float(ticker.get("ask") or 0.0)
    if ask <= 0:
        raise RuntimeError("No ask price.")

    base_amount = stake_quote / ask
    base_amount = amount_to_precision_safe(ex, market, base_amount)

    min_amount, min_cost = market_limits(ex, market)
    if min_cost and stake_quote < min_cost:
        raise RuntimeError(f"Stake {stake_quote:.2f} below min cost {min_cost:.2f}")
    if min_amount and base_amount < min_amount:
        raise RuntimeError(f"Amount {base_amount:.8f} below min amount {min_amount}")

    if dry_run:
        filled = float(base_amount)
        avg = float(ask)
        cost = float(filled * avg)
        fake = {"id": "DRYRUN", "filled": filled, "average": avg, "cost": cost, "fee": {"cost": cost * (taker_fee_pct / 100.0)}}
        fee_q = fee_from_order_or_estimate(fake, cost, taker_fee_pct)
        return fake, filled, avg, cost, fee_q

    order = ex.create_order(market, "market", "buy", base_amount)
    filled = float(order.get("filled") or base_amount)
    avg = float(order.get("average") or ask)
    cost = float(order.get("cost") or (filled * avg))
    fee_q = fee_from_order_or_estimate(order, cost, taker_fee_pct)
    return order, filled, avg, cost, fee_q

def place_market_sell(ex, market: str, base_amount: float, taker_fee_pct: float, dry_run: bool):
    ticker = fetch_ticker(ex, market)
    bid = float(ticker.get("bid") or 0.0)
    if bid <= 0:
        raise RuntimeError("No bid price.")

    base_amount = amount_to_precision_safe(ex, market, base_amount)

    if dry_run:
        filled = float(base_amount)
        avg = float(bid)
        proceeds = float(filled * avg)
        fake = {"id": "DRYRUN", "filled": filled, "average": avg, "cost": proceeds, "fee": {"cost": proceeds * (taker_fee_pct / 100.0)}}
        fee_q = fee_from_order_or_estimate(fake, proceeds, taker_fee_pct)
        return fake, filled, avg, proceeds, fee_q

    order = ex.create_order(market, "market", "sell", base_amount)
    filled = float(order.get("filled") or base_amount)
    avg = float(order.get("average") or bid)
    proceeds = float(order.get("cost") or (filled * avg))
    fee_q = fee_from_order_or_estimate(order, proceeds, taker_fee_pct)
    return order, filled, avg, proceeds, fee_q

# ------------------ Skip logging ------------------

_skip_last: Dict[str, float] = {}

def log_skip(market: str, reason_key: str, msg: str, every_s: int):
    key = f"{market}:{reason_key}"
    now = time.time()
    last = _skip_last.get(key, 0.0)
    if now - last >= every_s:
        _skip_last[key] = now
        LOG.info(f"{market} {msg}")

# ------------------ Main ------------------

def main():
    load_dotenv()
    cfg = load_cfg()
    setup_logging(cfg["logging"]["level"])

    quote = cfg["quote"]
    timeframe = cfg["timeframe"]

    sc = cfg["scanner"]
    rk = cfg["risk"]
    fees = cfg["fees"]
    lg = cfg["logging"]

    stake = float(rk["fixed_stake_quote"])
    max_open = int(rk["max_open_positions"])
    cooldown_min = int(rk["cooldown_minutes"])
    max_spread_pct = float(rk["max_spread_pct"])
    eur_reserve = float(rk["eur_reserve"])
    dry_run = bool(rk["dry_run"])
    skip_every = int(rk["skip_log_every_seconds"])
    min_profit_eur = float(rk["min_profit_eur"])
    taker_fee_pct = float(fees["taker_fee_pct"])
    sleep_s = int(lg["loop_sleep_seconds"])
    candles_limit = int(lg["candles_limit"])

    LOG.info(f"Trend bot started. Config={cfg.get('_cfg_path')} DRY_RUN={dry_run}")
    LOG.info(
        f"Scanner: auto_scan={sc['auto_scan']} top_n_markets={sc['top_n_markets']} "
        f"min_quote_volume={sc['min_quote_volume']}"
    )
    LOG.info(
        f"Risk: stake={stake} {quote}, max_open={max_open}, cooldown={cooldown_min}m, "
        f"max_spread_pct={max_spread_pct}% , eur_reserve={eur_reserve} {quote}, min_profit_eur={min_profit_eur}"
    )

    ex = make_exchange()
    ex.load_markets()

    state = load_state()
    ensure_csv_header()

    scanned_bases = list(cfg.get("symbols", []))
    last_scan_ts = 0.0
    scan_refresh_seconds = max(300, timeframe_to_seconds(timeframe))

    while True:
        try:
            if sc["auto_scan"] and (time.time() - last_scan_ts >= scan_refresh_seconds or not scanned_bases):
                try:
                    scanned_bases = scan_markets(ex, quote, sc)
                    last_scan_ts = time.time()
                    LOG.info(f"Scanner selected {len(scanned_bases)} markets: {', '.join(scanned_bases[:40])}")
                except Exception as e:
                    LOG.warning(f"Scanner failed, fallback to config symbols: {e}")
                    scanned_bases = list(cfg.get("symbols", []))

            balance = safe_fetch_balance(ex, retries=5, base_sleep=1.0)
            free_quote = get_free_quote(balance, quote)
            LOG.info(f"Free {quote}: {fmt_eur(free_quote)}")

            state.setdefault("positions", {})
            state.setdefault("cooldown", {})

            open_markets = list((state.get("positions", {}) or {}).keys())
            open_bases = [m.split("/")[0].upper() for m in open_markets]

            loop_bases = []
            for b in scanned_bases + open_bases:
                if b not in loop_bases:
                    loop_bases.append(b)

            buy_allowed_bases = set(scanned_bases)

            for base in loop_bases:
                market = market_symbol(base, quote)
                positions = state.get("positions", {}) or {}
                in_pos = market in positions
                next_ok = float((state.get("cooldown", {}) or {}).get(market, 0) or 0)

                if market not in ex.markets:
                    continue

                try:
                    tkr = fetch_ticker(ex, market)
                    spr = calc_spread_pct(tkr)
                except Exception as e:
                    log_skip(market, "TICKER_ERROR", f"SKIP ticker error: {e}", skip_every)
                    continue

                if spr > max_spread_pct:
                    log_skip(market, "SPREAD_HIGH", f"SKIP spread {spr:.3f}% > {max_spread_pct:.3f}%", skip_every)
                    continue

                try:
                    df = fetch_candles(ex, market, timeframe, candles_limit)
                    s = compute_signal(df, cfg)
                except Exception as e:
                    log_skip(market, "CANDLES_ERROR", f"SKIP candles error: {e}", skip_every)
                    continue

                last_price = float(s["last_close"])

                # ---------- SELL ----------
                if in_pos:
                    pos = positions[market]
                    if not to_bool(pos.get("opened_by_bot", False), False):
                        continue

                    do_sell, reason = should_sell(s, pos, cfg, last_price)
                    if not do_sell:
                        continue

                    est_net_pnl = estimate_net_pnl_at_price(pos, last_price, taker_fee_pct)
                    if reason in ("SELL ATR_TP", "SELL TREND_BREAK") and est_net_pnl < min_profit_eur:
                        log_skip(
                            market,
                            "MIN_PROFIT",
                            f"SKIP min profit {est_net_pnl:.2f} < {min_profit_eur:.2f}",
                            skip_every,
                        )
                        continue

                    base_amount = float(pos["base_amount"])
                    try:
                        order, filled, avg, proceeds, sell_fee = place_market_sell(
                            ex, market, base_amount, taker_fee_pct, dry_run
                        )
                    except Exception as e:
                        log_skip(market, "SELL_FAILED", f"SELL failed: {e}", skip_every)
                        continue

                    entry_cost = float(pos.get("entry_cost_quote", 0.0))
                    entry_fee = float(pos.get("entry_fee_quote", 0.0))
                    net_pnl = (proceeds - entry_cost) - (entry_fee + sell_fee)

                    hold_min = 0.0
                    entry_ts = pos.get("entry_ts")
                    if entry_ts:
                        try:
                            t0 = datetime.fromisoformat(entry_ts)
                            hold_min = (datetime.now(timezone.utc).astimezone() - t0).total_seconds() / 60.0
                        except Exception:
                            hold_min = 0.0

                    state["pnl_quote"] = float(state.get("pnl_quote", 0.0)) + float(net_pnl)
                    state["trades"] = int(state.get("trades", 0)) + 1
                    if net_pnl > 0:
                        state["wins"] = int(state.get("wins", 0)) + 1

                    state["cooldown"][market] = time.time() + cooldown_min * 60
                    del state["positions"][market]
                    save_state(state)

                    append_tx({
                        "ts": now_iso(),
                        "market": market,
                        "side": "SELL",
                        "price": avg,
                        "base_amount": filled,
                        "fees_quote": (entry_fee + sell_fee),
                        "spread_pct": spr,
                        "net_pnl_quote": net_pnl,
                        "holding_time_min": hold_min,
                        "reason": reason,
                        "dry_run": dry_run,
                    })

                    winrate = (state["wins"] / state["trades"] * 100.0) if state["trades"] else 0.0
                    LOG.info(
                        f"{market} {reason} | PnL {quote} {fmt_eur(net_pnl)} | total {quote} {fmt_eur(state['pnl_quote'])} "
                        f"| trades {state['trades']} winrate {winrate:.1f}% | hold {hold_min:.1f}m"
                    )

                    balance = safe_fetch_balance(ex, retries=3, base_sleep=0.8)
                    free_quote = get_free_quote(balance, quote)
                    continue

                # ---------- BUY ----------
                if base not in buy_allowed_bases:
                    continue

                if len(state.get("positions", {}) or {}) >= max_open:
                    log_skip(
                        market,
                        "MAX_OPEN",
                        f"SKIP max_open_positions reached ({len(state.get('positions', {}) or {})}/{max_open})",
                        skip_every,
                    )
                    continue

                if time.time() < next_ok:
                    continue

                if free_quote < (stake + eur_reserve):
                    log_skip(
                        market,
                        "RESERVE",
                        f"SKIP reserve (free {fmt_eur(free_quote)} < stake+reserve {fmt_eur(stake + eur_reserve)})",
                        skip_every,
                    )
                    continue

                ok, reason_key, reason_msg = should_buy(s, False, cfg)
                if not ok:
                    log_skip(market, reason_key, reason_msg, skip_every)
                    continue

                try:
                    order, filled, avg, cost, buy_fee = place_market_buy(
                        ex, market, stake, taker_fee_pct, dry_run
                    )
                except Exception as e:
                    log_skip(market, "BUY_FAILED", f"BUY failed: {e}", skip_every)
                    continue

                state["positions"][market] = {
                    "opened_by_bot": True,
                    "base_amount": filled,
                    "entry_price": avg,
                    "entry_ts": datetime.now(timezone.utc).astimezone().isoformat(timespec="seconds"),
                    "entry_cost_quote": cost,
                    "entry_fee_quote": buy_fee,
                }
                save_state(state)

                append_tx({
                    "ts": now_iso(),
                    "market": market,
                    "side": "BUY",
                    "price": avg,
                    "base_amount": filled,
                    "fees_quote": buy_fee,
                    "spread_pct": spr,
                    "net_pnl_quote": 0.0,
                    "holding_time_min": 0.0,
                    "reason": reason_msg,
                    "dry_run": dry_run,
                })

                LOG.info(f"{market} BUY | stake {quote} {fmt_eur(stake)} @ {avg:.8f} | spread {spr:.3f}%")

                balance = safe_fetch_balance(ex, retries=3, base_sleep=0.8)
                free_quote = get_free_quote(balance, quote)

            time.sleep(sleep_s)

        except KeyboardInterrupt:
            LOG.info("Stopped by user.")
            break
        except Exception as e:
            LOG.error(f"Main loop error: {e}")
            time.sleep(10)

if __name__ == "__main__":
    main()
